<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>방명록</title>
    <style>
        fieldset{
            margin-bottom: 20px;
        }
    </style>
  </head>
  <body>
    <fieldset>
        <legend>방명록 등록</legend>
        <input type="text" placeholder="사용자 이름"><br>
        <input type="text" placeholder="방명록"><br>
        <button type="button" onclick="write()">등록</button>
    </fieldset>

    <table border="1" cellspacing="0">
      <thead>
        <tr>
          <th>ID</th>
          <th>작성자</th>
          <th>방명록</th>
          <th>수정</th>
          <th>삭제</th>
        </tr>
      </thead>
      <tbody>
        <% for (let i=0;i<data.length; i++) { %>
        <tr id="tr_ <%= data[i].id %>">
            <td><a href="/visitor/<%= data[i].id%>"><%= data[i].id %></a></td>
            <td><%= data[i].name %></td>
            <td><%= data[i].comment %></td>
            <td><button type="button" onclick="edit()">수정</button></td>
            <td><button type="button" onclick="delte()">삭제</button></td>
        </tr>
        <% } %>
      </tbody>
    </table>
    <a href="/">홈으로 돌아가기</a>
    <script>

    </script>
  </body>
</html>